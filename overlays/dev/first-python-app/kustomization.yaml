apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../../base/charts

helmCharts:
- name: first-python-app
  releaseName: first-python-app-dev
  valuesFile: ./patch-values.yaml